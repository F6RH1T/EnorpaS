# ENORPA Brülör Kontrol V19

Bu sürümde PDF raporundaki datasheet teknik çizimleri doğrudan `app.js` içine gömülmüştür.
Böylece GitHub klasör yolu, eksik asset yükleme veya tarayıcı önbelleği nedeniyle kırık görsel oluşmaz.

## GitHub güncellemesi
Yalnızca `app.js`, `sw.js` ve `BRULOR_KATALOGU_V19.csv` dosyalarını ana dizine yüklemek yeterlidir.
Eski `BRULOR_KATALOGU_V18.csv` dosyası silinebilir.
